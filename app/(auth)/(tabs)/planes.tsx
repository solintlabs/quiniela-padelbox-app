/**
 * Pestaña Planes de la zona pre-login: la MISMA pantalla de planes de la app
 * (/api/saas/config es público, funciona sin sesión). Sin slug no enseña el
 * botón de pago: solo informa — y de vender se encarga.
 */
export { default } from '../../planes';
